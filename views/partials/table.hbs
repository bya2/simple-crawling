  <table>
    <!-- Column Titles --> 
    <tr>
      {{#each metaData}}
        <th>
          {{this}}
        </th>
      {{/each}}
    </tr>

    <!-- Rows -->
    {{#each docs}}
      <tr class="contents">
        <td>
          {{this._doc.commentId}}
        </td>
        <td>
          {{this._doc.nickname}}
        </td>
        <td>
          {{this._doc.content}}
        </td>
        <td>
          {{this._doc.rate}}
        </td>
        <td>
          {{this._doc.updated}}
        </td>
        <td>
          <a href="/{{this._doc.productId}}">
            go
          </a>
        </td>
      </tr>
    {{/each}}
  </table>